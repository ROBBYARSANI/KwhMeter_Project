import MapView, { Marker, Circle } from 'react-native-maps';

// Re-export the native components so other modules can import them
export default MapView;
export { MapView };
export { Marker };
export { Circle };
